Create Base Box

vagrant package --base my-virtual-machine

Import Base Box

vagrant box add --name my-box /path/to/the/new.box
vagrant init my-box
vagrant up


Difference between VDI and VMDK (resizing)

Terraform Ansible Vagrant Template File Inventory

Start/stop k3s server and k3s agents