#auto-generated by vagrant role

k3s_control_nodes:
  hosts:
    {{ hosts["host1"].host }}:
      ansible_host: {{ hosts["host1"].ip }}
  vars:
    ansible_user: vagrant
k3s_worker_nodes:
  hosts:
{% for host in hosts %}
{% if host != "host1" %}
    {{ hosts[host].host }}:
      ansible_host: {{ hosts[host].ip }}
{% endif %}
{% endfor %}
  vars:
    ansible_user: vagrant
