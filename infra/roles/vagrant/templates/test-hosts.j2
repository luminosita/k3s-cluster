#auto-generated by vagrant role

{% set f_env = '%s' | format(env) %}
{% set hosts = inventory[f_env].hosts %}

{% for key, value in hosts.items() %}
    {% set name = key %}
    {% set f_name = [name, "-", env] | join("") %}
    {% set dhcp_ip = dhcp_ips[f_name] %}

echo 'Exec remote host scripts for server {{f_name}} at vagrant@{{dhcp_ip}}:/home/vagrant/'

ssh -i ~/.vagrant.d/insecure_private_key vagrant@{{dhcp_ip}} 'echo "Hello Moto" :-) :-) :-)'

{% endfor %}
